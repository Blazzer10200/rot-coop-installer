{
  "schemaVersion": 1,
  "updated": "2026-07-09",
  "profiles": [
    {
      "id": "rot71-coop",
      "name": "Realm of Thrones 7.1 + Bannerlord Together (Co-op)",
      "gameVersion": "1.3.15.110062",
      "gameVersionShort": "1.3.15",
      "warSails": false,
      "notes": "The verified-working GoT co-op stack. ROT 7.1 = newest non-Warsails 1.3.15 build.",
      "mods": {
        "ROT": {
          "version": "7.1",
          "modules": ["ROT-Core", "ROT-Content", "ROT_Map", "ROT-Dragon"],
          "source": "https://www.nexusmods.com/mountandblade2bannerlord/mods/2907",
          "altSource": "https://www.moddb.com/mods/realm-of-thrones/downloads",
          "install": "manual-extract",
          "notes": "Use the 'ROT 7.1 for Bannerlord 1.3.15' file. NOT 8.0 Warsails. Extract w/ 7-Zip. Folder ROT-Map must be renamed to ROT_Map (its internal Id)."
        },
        "BannerlordTogether": {
          "version": "0.4.1.2",
          "modules": ["BannerlordTogether"],
          "source": "https://www.nexusmods.com/mountandblade2bannerlord/mods/10426",
          "install": "extract-to-modules",
          "coopStart": {
            "host": "Main menu -> click 'Host Coop' FIRST, then start a new campaign, finish character + clan creation, reach the map. (Do NOT use the plain singleplayer New Campaign button.)",
            "join": "Main menu -> 'Join Co-op' -> enter host's IP or join via Steam lobby -> pick/create a character. Host's save transfers automatically.",
            "networking": "Steam networking = no port-forward needed (easiest). Non-Steam internet play: host forwards UDP 47770 (and UDP 47771 for live battles). LAN: use host's ipconfig IPv4. VPN (Radmin/Hamachi) works as a fallback.",
            "requirements": "Both players: exact game v1.3.15.110062, War Sails OFF, identical mod set + versions. A yellow launcher caution = normal (click Yes); a red incompatible warning = wrong game version."
          }
        },
        "BLSE": {
          "version": "1.6.7",
          "minVersion": "1.6.0",
          "source": "https://www.nexusmods.com/mountandblade2bannerlord/mods/1",
          "install": "extract-to-gameroot"
        },
        "deps": {
          "bundle": "OFFICIAL-BUTR",
          "modules": ["Bannerlord.Harmony", "Bannerlord.UIExtenderEx", "Bannerlord.ButterLib", "Bannerlord.MBOptionScreen"],
          "versions": { "Bannerlord.Harmony": "2.4.2.225", "Bannerlord.ButterLib": "2.10.3", "Bannerlord.UIExtenderEx": "2.13.2", "Bannerlord.MBOptionScreen": "5.11.3" },
          "sources": {
            "Harmony": "https://www.nexusmods.com/mountandblade2bannerlord/mods/2006",
            "ButterLib": "https://www.nexusmods.com/mountandblade2bannerlord/mods/2018",
            "UIExtenderEx": "https://www.nexusmods.com/mountandblade2bannerlord/mods/2102",
            "MCM": "https://github.com/Aragas/Bannerlord.MBOptionScreen/releases/tag/v5.11.3"
          },
          "install": "extract-each-to-modules",
          "mcm": "MCM (Bannerlord.MBOptionScreen) is REQUIRED for ROT, not optional. ROT.dll calls MCMv5 / MCM.Abstractions at runtime even though ROT-Core's SubModule.xml does NOT declare it as a dependency (so the launcher won't warn). Without MCM present, ROT throws an unhandled exception 0xE0434352 (Resolving: MCMv5 5.11.3.0 -> crash) ~26s into loading a new game. Use OFFICIAL MCM v5.11.3 (assembly 5.11.3.0, matches what ROT resolves); do NOT use the ModReady/BetaDeps MCM stub (drags in BetaDeps.Foundation.dll which conflicts with official ButterLib).",
          "CRITICAL": "Use OFFICIAL BUTR deps, NOT ModReady/BetaDeps stubs. The ModReady 1.0.5 bundle is built for Bannerlord 1.4.x - on 1.3.15 its stub Harmony patches target the wrong method signatures. They REACH THE MAIN MENU but silently fail at campaign-init: BetaDeps catches the MissingMethodException, unpatches, and the new-game bootstrap loops forever ('Initializing new game begin' x thousands, no crash, clean errors log). Official ButterLib ships a per-game-version implementation DLL (matches 1.3.x) and needs BLSE.AssemblyResolver, which BLSE provides. This SUPERSEDES the old 'stubs reach the menu' note - reaching the menu was never the bar; loading a campaign is."
        }
      },
      "loadOrder": [
        "Native", "SandBoxCore", "Sandbox", "StoryMode", "CustomBattle",
        "Bannerlord.Harmony", "Bannerlord.UIExtenderEx", "Bannerlord.ButterLib", "Bannerlord.MBOptionScreen",
        "ROT-Core", "ROT-Content", "ROT_Map", "ROT-Dragon",
        "BannerlordTogether"
      ],
      "_moduleIdFixupsNote": "Format is \"oldFolderName\": \"correctFolderName\". If ROT ships a folder whose name doesn't match its internal module Id, the tool flags it to be renamed to the correct name.",
      "moduleIdFixups": { "ROT-Map": "ROT_Map" }
    }
  ],
  "knownCrashes": [
    {
      "signature": "Initializing new game begin (repeating thousands of times, no crash, endless load) - PRIMARY cause",
      "exception": "none - silent infinite loop (native-level, not a .NET exception, so errors log is CLEAN)",
      "cause": "The ModReady/BetaDeps stub dependency bundle (v1.0.5) is built for Bannerlord 1.4.x. On 1.3.15 its stub Harmony patches target the wrong method signatures. The mods load and REACH THE MAIN MENU, but at campaign-init a patch hits a MissingMethodException; BetaDeps catches it, synthesizes a fallback, and silently unpatches - so the new-game bootstrap can never complete and loops forever. Reaches menu, hangs on new campaign.",
      "fix": "swap-to-official-butr-deps",
      "severity": "fatal-silent",
      "verified": "Live log rgl_log_5008 (2026-07-02): 866+ init loops in 8 min, ZERO unique progress after startup, clean errors log, BetaDeps.Harmony.dll/BetaDeps.Foundation.dll loaded. Confirmed still looping. Fix = official Harmony 2.4.2/ButterLib 2.10.3/UIExtenderEx 2.13.2 (needs BLSE, now installed)."
    },
    {
      "signature": "Initializing new game loop WITH 'duplicate key sequence' errors - SECONDARY cause (also fixed)",
      "exception": "none - silent loop, but log shows duplicate-key errors",
      "logMarker": "duplicate key sequence '...' for the 'string_unique_attribute' key",
      "cause": "ROT 7.1 ships GameText string files (comment_strings.xml, ROT_module_strings.xml) with DUPLICATE <string id> entries + empty <tag/> elements, violating GameText.xsd. The string-table build aborts on the unique-key check and init retries. This is a SEPARATE loop trigger from the stub-deps one; fixing the XML removes the duplicate-key errors but the stub-deps loop remains until deps are swapped.",
      "fix": "repair-rot-xml",
      "severity": "fatal-silent",
      "verified": "Log rgl_log_28844/9924: duplicate-key errors on ROT-Content string files. After repair-rot-xml, live log rgl_log_5008 showed ZERO duplicate-key errors (fix confirmed) - but the deeper stub-deps loop then surfaced."
    },
    {
      "signature": "Could not load file or assembly 'NavalDLC' - instant crash before the menu (FileNotFoundException at ROT.SubModule.OnSubModuleLoad)",
      "exception": "System.IO.FileNotFoundException: Could not load file or assembly 'NavalDLC, Version=1.0.0.0'",
      "cause": "ROT 8.x is the WARSAILS edition: its OnSubModuleLoad Harmony-patches NavalDLC.GameComponents.NavalDLCMapDistanceModel - a hard reference to the War Sails DLC assembly. Without the DLC's NavalDLC module installed the assembly can't resolve and the game dies before the main menu. ROT 7.1 has no NavalDLC reference and runs fine without the DLC. NOTE: deps can all be perfect and this still crashes - it's a ROT-build problem, not a dependency problem.",
      "fix": "install-rot-71-non-warsails (installing + enabling the War Sails DLC also stops the crash, but ROT 8.x cannot co-op with a 7.1 host - stick to 7.1 for this stack)",
      "severity": "fatal-instant",
      "verified": "Real BLSE crash report 2026-07-09: ROT-Core v8.0.10 on game 1.3.15, no NavalDLC module loaded, FileNotFoundException from ROT.SubModule.OnSubModuleLoad. All four BUTR deps were the correct official versions. Detection gap (no ROT-version check anywhere) now covered in Preflight, Validate, Show-Status, Show-Recommendation + the crash-report reader."
    },
    {
      "signature": "Shader cache version of the external module (ROT-Content) is invalid",
      "exception": "0xC0000005",
      "cause": "Mismatched/corrupt precompiled shader cache (.sack)",
      "fix": "delete-shader-caches"
    },
    {
      "signature": "Unable to initialize Steam API",
      "cause": "Steam not running or not logged in",
      "fix": "start-steam-first"
    }
  ],
  "_note": "shaderCachePaths + knownCrashes below are human-readable documentation. The tool's actual shader-cache list lives in src/Common.ps1 (Get-ShaderCachePaths), which covers all four ROT modules; keep the two in sync when editing.",
  "shaderCachePaths": [
    "{MODULES}\\ROT-Core\\Shaders\\D3D11\\compressed_shader_cache.sack",
    "{MODULES}\\ROT-Content\\Shaders\\D3D11\\compressed_shader_cache.sack",
    "{MODULES}\\ROT_Map\\Shaders\\D3D11\\compressed_shader_cache.sack",
    "{MODULES}\\ROT-Dragon\\Shaders\\D3D11\\compressed_shader_cache.sack",
    "{PROGRAMDATA}\\Mount and Blade II Bannerlord\\Shaders"
  ]
}
